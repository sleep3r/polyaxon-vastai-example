# Experiment configuration
# Override via polyaxonfile inputs or CLI: --epochs=10 --lr=0.0005

model:
  hidden_size: 128
  dropout: 0.2

training:
  epochs: 5
  batch_size: 64
  lr: 0.001
  weight_decay: 1.0e-5
  log_interval: 100  # log every N batches

data:
  num_workers: 2
